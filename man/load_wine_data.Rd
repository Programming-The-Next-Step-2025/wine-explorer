% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{load_wine_data}
\alias{load_wine_data}
\title{Loads wine data in a standardised form (project dataset or a given path)}
\usage{
load_wine_data(
  path = system.file("extdata", "WineDataset.csv", package = "vinexplorer")
)
}
\arguments{
\item{path}{Path to the CSV. Defaults to the package file
\code{inst/extdata/WineDataset.csv}.}
}
\value{
A tibble with exactly the 12 standardised columns.
}
\description{
Returns only these standardised columns (in that order):
wine_id, wine_name, type, region, country, grape,
secondary_grapes, vintage, price, appellation, style, characteristics.
}
\details{
Notes:
\itemize{
\item Vintage is normalised: "NV" to NA; "2019/20" to 2019 (integer).
\item Type is lower-cased and "rosé" is normalised to "rose").
\item Region/Country are trimmed for stray spaces.
}
}
\examples{
df <- vinexplorer::load_wine_data()
names(df); head(df)
}
